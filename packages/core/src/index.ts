// @executagent/core — shared domain libraries.
// Modules are added per build-order step (logger, errors, hash, carbon, pii, schemas).
export const CORE_VERSION = "0.1.0";

export type {
  Database,
  Json,
} from "./database.types.ts";
